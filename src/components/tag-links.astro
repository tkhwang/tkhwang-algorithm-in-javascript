---
import { FRONTMATTER_TAGS, type FrontmatterTag } from '@constants'
import Link from './link.astro'

interface Props {
	tags: FrontmatterTag[]
	class?: string
	badgeClass?: string
}

const { tags, class: className = '', badgeClass = '' } = Astro.props
---

{
	tags.length ? (
		<div class={`flex flex-wrap gap-2 ${className}`.trim()}>
			{tags.map((tag) => (
				<Link
					class={`inline-flex items-center rounded-full bg-blue-100 text-blue-800 px-2 py-0.5 text-xs font-semibold transition-colors hover:bg-blue-200! hover:text-blue-900! dark:bg-blue-500/20 dark:text-blue-100 dark:hover:bg-blue-500/40! dark:hover:text-blue-50! no-underline! hover:no-underline! focus-visible:no-underline! ${badgeClass}`.trim()}
					data-pagefind-filter="tag"
					href={`/tags/${tag}/`}
				>
					{tag}
				</Link>
			))}
		</div>
	) : null
}
